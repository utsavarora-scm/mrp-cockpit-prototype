/**
 * The product's name, in one place.
 *
 * It appears in the top bar on every screen, so it is the one string a rename
 * has to reach. "MRP" is deliberately absent: the calculation is not what this
 * adds, and the name of the system that already does it does not belong across
 * the top of every frame.
 */
export const PRODUCT_NAME = 'Planning & Supply Schedule';
