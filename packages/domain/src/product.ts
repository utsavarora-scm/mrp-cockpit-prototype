/**
 * The product's name, in one place.
 *
 * It appears in the top bar on every screen, so it is the one string a rename
 * has to reach. "MRP" is deliberately absent: the client's own framing was
 * "forget that we have an MRP", and their existing system's name does not
 * belong across the top of every frame of the demo.
 */
export const PRODUCT_NAME = 'Planning & Procurement Cockpit';
