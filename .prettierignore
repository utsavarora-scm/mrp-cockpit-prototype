node_modules
dist
build
.turbo
.next
.env
pnpm-lock.yaml

# Regenerated by `next build`, and already gitignored. Formatting it is a
# check that fails on a file nobody wrote and nobody can keep formatted.
next-env.d.ts
